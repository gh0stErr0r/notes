#import "@preview/grape-suite:3.1.0": exercise
#import exercise: project

#set text(lang: "ru")

#show: project.with(
  title: [Дискретная математика],
  author: "Лекции",
  document-title: [Дискретная математика],
  show-outline: false,
  show-todolist: false,
  header: [],
  footer: [],
  show-header-line: false,
  show-footer-line: false,
  page-margins: (top: 2.2cm, bottom: 2cm, x: 2.2cm),
  text-font: ("Libertinus Serif", "FreeSerif"),
  math-font: ("New Computer Modern Math", "DejaVu Math TeX Gyre"),
  box-definition-title: [Определение],
  box-notice-title: [Замечание],
  box-example-title: [Пример],
)

#set par(
  justify: true,
  leading: 0.72em,
  first-line-indent: 1.2em,
  spacing: 0.35em,
)
#set heading(numbering: "1.", outlined: true)

#let purple = rgb("#373fcf")
#let purple-soft = rgb("#f4f5ff")
#let gray = rgb("#6d6d6d")
#let gray-soft = rgb("#fafafa")
#let amber = rgb("#8a5a1f")
#let amber-soft = rgb("#fff8ee")
#let green = rgb("#356b39")
#let green-soft = rgb("#f1f7f1")
#let ink = rgb("#2b2b2b")

#let simple-box(
  title,
  body,
  line_color,
  fill_color,
  title_color: auto,
  dotted: false,
) = [
  #let actual_title_color = if title_color == auto { line_color } else {
    title_color
  }
  #set par(first-line-indent: 0pt)
  #block(
    above: 0.35em,
    below: 0.45em,
    inset: 0.85em,
    fill: fill_color,
    stroke: (
      left: (
        thickness: 3pt,
        paint: line_color,
        dash: if dotted { "dotted" } else { "solid" },
      ),
    ),
  )[
    #text(8.4pt, weight: "bold", fill: actual_title_color)[#smallcaps(title)]
    #v(0.25em)
    #body
  ]
]

#let theorem(body) = simple-box([Теорема], body, rgb("#8d0000"), rgb("#fcf7f7"))
#let def(body) = simple-box([Определение], body, purple, purple-soft)
#let note(body) = simple-box([Замечание], body, gray, gray-soft, dotted: true)
#let example(body) = simple-box(
  [Пример],
  body,
  amber,
  amber-soft,
  title_color: amber,
)
#let algo(body) = simple-box(
  [Алгоритм],
  body,
  green,
  green-soft,
  title_color: green,
)

#set par(first-line-indent: 0pt)
#text(14pt, weight: "semibold", fill: purple)[Оглавление]
#v(0.45em)
#outline(indent: 1.5em)
#v(0.8em)

#pagebreak()
= Лекция 1

== Множества

#def[
  Множество — это набор определенных и различных объектов нашей интуиции или интеллекта, рассматриваемый как единое целое (определение Кантора).
]

Элементами множества называются эти объекты.

$M = {m_1, m_2, m_3, m_4}$

$m_1 in M; m_5 ∉ M$

#note[
  $emptyset$ — обозначение пустого множества.
]

Способы задания множеств:

1. Перечислением: $M_2 = {1, 2, 3, 4, 5}$.
2. Указанием свойств: $M_2 = {m_i | m_i - "целое"; 1 <= m_i <= 5}$.

== Подмножества

#def[
  $M' subset M <-> M' = {m_i | m_i in M}, M' != M$ — $M'$ является подмножеством $M$ тогда и только тогда, когда каждый элемент множества $M'$ принадлежит $M$.
]

#note[
  $M'' subset.eq M$ — подмножество $M''$ может совпадать с множеством $M$.
]

== Операции с множествами

$M_a times M_b = {(m_i, m_j) | m_i in M_a, m_j in M_b}$

#def[
  Последовательностями элементов называются множества с четко зафиксированным порядком.
]

${a, b, c} != {c, a, b}$

$M^2$ называют декартовым произведением $M_a times M_b$, где $M_a = M_b = M$.

Бинарным отношением ($R_2$) называется подмножество $M^2$.

$R_2 subset M^2$

#def[
  Совокупность — множество, состоящее из элементов разной структуры: $<M, R_2>$.
]

#def[
  Мощность множества — количество элементов множества.
]

$|M^2_2| = 25$

#def[
  Окрестностью единичного радиуса элемента $m_i$ называется множество элементов $m_j in M$, для которых $(m_i, m_j) in R_2$, где $R_2 subset M^2$.
]

== Графы

#def[
  $G = <M, R_2>$ — граф, где $M$ — носитель (множество вершин), а $R_2$ — сигнатура (множество дуг или ребер).
]

=== Ориентированный граф

#figure(
  image("data/orient_graph.svg", width: 6cm),
  caption: [Ориентированный граф],
)

#note[
  Перечисление:
]

$G = <V, U>$

$V = {a, b, c, d}$

$U = {(a, b), (b, c), (b, d), (d, c)}$

#note[
  Матричное представление (через матрицу смежности):
]

$S = mat(
  \\, a, b, c, d;
  a, , 1, , ;
  b, , , 1, 1;
  c, , , , ;
  d, , , 1, ;
)$


=== Неориентированный граф

#figure(
  image("data/not_orient_graph.svg", width: 6cm),
  caption: [Неориентированный граф],
)

#note[
  Перечисление:
]

$G = <V, U>$

$V = {a, b, c, d}$

$U = {{a, b}, {b, c}, {b, d}, {c, d}}$

#note[
  Матричное представление (через матрицу смежности):
]

$S = mat(
  \\, a, b, c, d;
  a, 0, 1, 0, 0;
  b, 1, 0, 1, 1;
  c, 0, 1, 0, 1;
  d, 0, 1, 1, 0;
)$

#def[
  Фактор-множество — это множество окрестностей, взятых для всех элементов при задании в нем бинарного отношения.
]

$mat(
  a, b, c, d;
  {b}, {d, c}, {emptyset}, {c};
)$

#pagebreak()

= Лекция 2

#def[
  Для каждого множества $M$ существует множество $B$, элементами которого являются подмножества множества $M$, и только они.
]

#def[
  Такое множество $B$ будем называть семейством множества $M$.
]

#def[
  Булеан — это множество, элементами которого являются всевозможные подмножества множества, которое принято обозначать $1$,
  где $1$ — универсальное множество (универсум) пространства, характерного для определенной задачи или области.
]

#example[
  $1 = {a, b, c}$

  $B(1) = {emptyset, {a}, {b}, {c}, {a, b}, {a, c}, {b, c}, 1}$
]

#def[
  $F subset M_y times M_x$ называется функцией, если для каждого элемента $x in M_x$ найдется не более одного элемента $y in M_y$.
]

#def[
  Функция называется полностью определенной, если для каждого элемента $x in M_x$ найдется $y in M_y$ такой, что $(y, x) in F$.
]

#def[
  $F subset M_y times M_x$ называется неполностью определенной функцией, если хотя бы для одного $x in M_x$ не найдется ни одного $y in M_y$ такого, что $(y, x) in F$.
]

#def[
  Если множество $M_x$ в определении $y = F(x)$ является декартовым произведением множеств $M_x_1 times M_x_2 ... times M_x_n$, то получим определение $N$-местной функции.
]

#note[
  Частным случаем $N$-местной функции является $N$-местная операция.
]

$y = F(x_1, x_2, ..., x_n)$, где $M_x_1 = M_x_2 = ... = M_x_n = M_y$; это значит, что области определения аргументов и область значений совпадают.

$M = M_a or M_b = {m_i | m_i in M_a, M_b}$

$M = M_a and M_b = {m_i | m_i in M_a and m_i in M_b}$

$M = M_a$ `\` $M_b = {m_i | m_i in M_a and m_i ∉ M_b}$

$not M = {m_i | m_i ∉ M}$

#def[
  Изоморфизм — это полное взаимно однозначное соответствие элементов изоморфных систем.
]

#note[
  Система изоморфна, если ее элементы и связи могут быть поставлены во взаимно однозначное соответствие.
]

#pagebreak()

= Лекция 3

== Внешняя положительная и отрицательная устойчивость графа

#figure(
  image("data/лекция3_граф1.jpg", width: 40%),
  caption: [Лекция 3: граф 1],
)

$G = <V, U>$

#def[
  Внешняя положительная устойчивость — это множество вершин (минимальное с точки зрения критичности), которые передают информацию во все вершины графа по пути длины 1. Предполагается, что передающая вершина этой информацией уже обладает.
]

#def[
  Внешняя отрицательная устойчивость — это множество вершин (минимальное с точки зрения критичности), которые собирают информацию из всех вершин графа по пути длины 1. Предполагается, что собирающая вершина этой информацией уже обладает.
]

#note[
  Свойство критичности для множества означает, что при удалении хотя бы одного элемента данного множества его основное свойство теряется.
]

#figure(
  image("data/лекция3_граф2.jpg", width: 20%),
  caption: [Лекция 3: граф 2],
)

$B^+У:$

$M_1 = {V_3}$

$M_2 = {V_1, V_2}$

$M_3 = {V_1, V_4}$

#def[
  МАФ-запись — это форма записи выражения, при которой все внешние операции являются аппликативными, а все остальные — аддитивными.
]

#def[
  Покрытие — это множество внешней положительной устойчивости ($B^+У$).
]

#note[
  Число $B^+У$ — это минимальная мощность из всех внешних положительных устойчивостей.
]

#algo[
  1. $S' = S + E$.
  2. Получаем для МАФ логику передачи информации по пути длины 1.
  3. Переходим от МАФ к АМФ с помощью оптимального применения алгебры Буля.
  4. Записываем покрытие.
  5. Определяем число $B^+У$.
]

#def[
  Говорят, что строка покрывает столбец, если она содержит ненулевой элемент в этом столбце.
]

#def[
  Покрытием столбцов строками называется множество строк, которые:

  1. Покрывают все столбцы.
  2. Обладают свойством критичности.
]

#pagebreak()

= Лекция 4 и 5
Скип

#pagebreak()

= Лекция 6

Скоро будет добавлено.


#pagebreak()

= Лекция 7

Рассмотрим $f(x_1, x_2) = not ( not x_1 or not x_2)$

$f not in not l_0 , f not in not l_1 , f not in L$

$
  4) S ->
$
// Здесь надо вставит табличку
$f not in S$



$f(gamma_1 , gamma_2) = not f()$



#def[
  Гиперкуб - это математическая модель N мерного пространства заданное ввиде графа каждая вершина которого имеет уникальный двоичный идентификатор длины N (с N разрядами) и две вершины соединяются ребром тогда и только тогда , когда их идентификатор имеет различие  в одном разряде
]

#def[
  Расстояние по Хелингу - это кол-во различий в разрядах идентификаторах двух вершин
]


= Лекция 8

#def[

]
